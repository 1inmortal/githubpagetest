{
  "name": "node-express-postgresql-api",
  "version": "1.0.0",
  "description": "Backend en Node.js con Express y PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "init-db": "node init-db.js",
    "lint": "echo 'No linting configured for server'",
    "db:migrate": "echo 'No migrations configured'",
    "db:seed": "echo 'No seeding configured'",
    "test:api": "echo 'No API tests configured'"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "pg": "^8.11.3"
  }
}
