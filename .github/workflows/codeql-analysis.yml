name: "CodeQL Analysis"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 1 * * 0'

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze code
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript', 'python' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4 # TODO: pin SHA
      with:
        fetch-depth: 0    # Necesario para CodeQL

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3 # TODO: pin SHA
      with:
        languages: ${{ matrix.language }}
        setup: advanced

    - name: Set up Node.js (for JavaScript analysis)
      if: matrix.language == 'javascript'
      uses: actions/setup-node@v4 # TODO: pin SHA
      with:
        node-version: '18'

    - name: Install Node dependencies
      if: matrix.language == 'javascript'
      run: |
        if [ -f "config/package.json" ]; then
          cd config && npm install
        fi
        if [ -f "src/components/react-ui-login/package.json" ]; then
          cd src/components/react-ui-login && npm install
        fi

    - name: Set up Python
      if: matrix.language == 'python'
      uses: actions/setup-python@v4 # TODO: pin SHA
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      if: matrix.language == 'python' && hashFiles('config/requirements.txt') != ''
      run: |
        pip install --upgrade pip
        pip install -r config/requirements.txt

    - name: Install config package
      if: matrix.language == 'python' && hashFiles('config/setup.py') != ''
      run: pip install -e config

    - name: Autobuild
      uses: github/codeql-action/autobuild@v3 # TODO: pin SHA

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3 # TODO: pin SHA
      with:
        output: results.sarif
        category: "/language:${{matrix.language}}"
